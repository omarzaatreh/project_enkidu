# Secrets
.env
.env.*
!.env.example

# Client competitive data never enters git history (eng review OV-6)
/results/
results.jsonl
/reports/

# Real client configs stay local; only *.example.json templates are committed
/config/*.json
!/config/*.example.json

# Business strategy / roadmap stays local (names real clients + competitors)
/TODOS.md

# Node
node_modules/
dist/

# Next.js (cockpit)
.next/
next-env.d.ts
*.tsbuildinfo

# OS
.DS_Store
.gstack/
