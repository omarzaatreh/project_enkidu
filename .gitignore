# Secrets
.env
.env.*
!.env.example

# Client competitive data never enters git history (eng review OV-6).
# Anchored to the repo root so the top-level data dirs are ignored WITHOUT
# also hiding source dirs of the same name (e.g. the app/api/reports route).
/results/
results.jsonl
/reports/

# Node
node_modules/
dist/

# Next.js (cockpit)
.next/
next-env.d.ts
*.tsbuildinfo

# OS
.DS_Store
.gstack/
